import React from "react";
import CreateTimelineContainer from "../createPage/createTimelineContainer";

const CreateTimelinePage = () => {
  return <CreateTimelineContainer />;
};

export default CreateTimelinePage;
